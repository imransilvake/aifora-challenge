// angular
import { Component } from '@angular/core';

@Component({
	selector: 'app-root',
	template: `
		<!-- Router Outlet -->
		<router-outlet></router-outlet>
	`
})

export class AppComponent {
}
